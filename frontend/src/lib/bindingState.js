export const bindingState = (e, setState, nameIn) => {
    const { name, value } = e.target;
    if (name === nameIn) {
        setState(value);
    } else if (name === nameIn) {
        setState(value);
    }
};